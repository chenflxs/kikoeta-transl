# 功能索引

按功能定位当前实现代码。路径均相对于仓库根目录。

## 入口与界面

| 功能 | 位置 | 说明 |
| --- | --- | --- |
| Flutter 应用与页面导航 | `app/lib/main.dart` | 启动应用、侧边栏和页面切换。 |
| 系统托盘与窗口生命周期 | `app/lib/main.dart` | 最小化时隐藏窗口，提供显示窗口、停止任务和退出菜单。 |
| 应用状态与任务控制 | `app/lib/app_state.dart` | 保存设置、创建任务、轮询任务状态。 |
| HTTP 客户端 | `app/lib/services/engine.dart` | 调用本地 engine API。 |
| 任务页 | `app/lib/pages/task_page.dart` | 导入文件、阶段开关、任务进度和日志。 |
| 模型页 | `app/lib/pages/models_page.dart` | 配置 ASR、矫正与翻译模型。 |
| 听写参数页 | `app/lib/pages/params_page.dart` | 配置 CrispASR 参数。 |
| 模型参数页 | `app/lib/pages/model_params_page.dart` | 配置矫正、翻译 Prompt 与请求参数。 |
| 字典页 | `app/lib/pages/dict_page.dart` | 选择和编辑 GalTransl 字典。 |
| 输出页 | `app/lib/pages/output_page.dart` | 设置格式、输出目录和 kikoeta 歌词库。 |
| 设置页 | `app/lib/pages/settings_page.dart` | 设置工具路径、代理和远程访问。 |

## 服务与任务

| 功能 | 位置 | 说明 |
| --- | --- | --- |
| HTTP API | `engine/server.py` | 本地 API、远程任务 API、健康检查与服务生命周期。 |
| 远程文件下载 | `engine/kt/download.py` | 接收任务中的 HTTP/HTTPS `url`，下载到任务临时目录并在任务结束后清理。 |
| 命令行 | `engine/kt/__main__.py` | `kt serve` 与 `kt run` 命令。 |
| 任务队列 | `engine/kt/jobs.py` | 创建、取消和执行任务。 |
| 任务事件 | `engine/kt/events.py` | 向界面和 API 提供状态、日志事件。 |
| 任务取消 | `engine/kt/cancellation.py` | 在管线阶段检查取消信号，并终止转码、ASR 子进程。 |
| 设置读写 | `engine/kt/settings.py` | 读写 `engine/data/settings.json`，合并任务级覆盖设置。 |
| 数据模型 | `engine/kt/models.py` | 任务、阶段、字幕条目和各类设置模型。 |

远程客户端可通过 `POST /api/v1/jobs` 传入下载地址；kikoeta-transl 会先下载文件，再进入处理管线：

```json
{
  "files": [{"url": "https://example.com/song.mp3", "name": "song.mp3"}],
  "flags": {"enable_correct": false, "enable_translate": true}
}
```

`name` 可省略，kikoeta-transl 会使用 URL 路径中的文件名。仅接受 HTTP/HTTPS 地址；下载文件与 Base64 上传文件均在任务结束后删除。

## 处理管线

| 阶段 | 位置 | 说明 |
| --- | --- | --- |
| 管线编排 | `engine/kt/pipeline.py` | 判断媒体或字幕输入，并按顺序执行各阶段。 |
| 阶段计划 | `engine/kt/stages_plan.py` | 根据输入类型和开关生成进度阶段。 |
| 字幕解析 | `engine/kt/subtitle.py` | 解析 SRT、LRC、VTT、ASS、SSA，提取作品号。 |
| 媒体转码 | `engine/kt/stages/transcode.py` | 调用 ffmpeg 转为 16 kHz 单声道 WAV。 |
| ASR 听写 | `engine/kt/stages/asr.py` | 组装 CrispASR 命令、执行听写并读取结果。 |
| 听写矫正 | `engine/kt/stages/correct.py` | 调用 OpenAI 兼容接口修正字幕文本。 |
| 翻译 | `engine/kt/stages/translate.py` | 创建 GalTransl 工作区、生成配置并执行翻译。 |
| 字幕导出 | `engine/kt/stages/export.py` | 导出目标语言或双语 LRC / SRT，并可写入歌词库。 |
| 临时文件清理 | `engine/kt/cleanup.py` | 清理转码、ASR 等中间产物。 |

## 工具与资源

| 功能 | 位置 | 说明 |
| --- | --- | --- |
| 工具发现与模型列表 | `engine/kt/tools.py` | 查找 ffmpeg、CrispASR，读取字典和 OpenAI 模型列表。 |
| 路径常量 | `engine/kt/paths.py` | 定义目录、支持的媒体与字幕扩展名。 |
| 内置翻译引擎 | `engine/GalTransl/` | GalTransl 源码、插件、字典和翻译规范。 |
| 本地运行资源 | `bin/` | ffmpeg、CrispASR、llama-server 与本地模型。 |

## 测试

| 范围 | 位置 |
| --- | --- |
| engine 核心与任务流程 | `engine/tests/test_core.py` |
| 矫正流式响应 | `engine/tests/test_correction_stream.py` |
| engine 关闭流程 | `engine/tests/test_shutdown.py` |
